# .env file content :

LIVEKIT_URL=""
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""
SECRET_KEY="This-is-the-secret-key"
MONGO_URI=""
DB_NAME=""
GEMINI_API_KEY=""
DEEPGRAM_API_KEY=""
ELEVENLABS_API_KEY=""
ELEVENLABS_VOICE_ID=""

# place the env file in the backend folder 

# create a virtual env using conda (prefered) or other

# install requirements for backned : `pip install -r requirements.txt` than run server user "Python run.py"

# setup for frontend run `npm i` for running the server run "npm run dev"  

# ensure backed server starts first